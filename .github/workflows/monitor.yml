name: 画像監視システム

on:
  schedule:
    # 10分ごとに実行 (UTC時間)
    - cron: '*/10 0-12 * * *'  # 9:00-21:00 JST (UTC+9)の営業時間内のみ実行
  workflow_dispatch: # 手動実行も可能

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    
    steps:
      - name: チェックアウト
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Python環境セットアップ
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Chrome環境セットアップ
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable
          
      - name: Tesseract OCRインストール
        run: |
          sudo apt-get update
          sudo apt-get install -y tesseract-ocr tesseract-ocr-jpn
      
      - name: 依存関係インストール
        run: |
          pip install selenium pytesseract pillow
      
      - name: 監視実行（JavaScript実行待機付き）
        run: |
          python scripts/monitor.py
        env:
          DISPLAY: :99
        continue-on-error: true
      
      - name: 結果確認
        if: always()
        run: |
          if [ -f "results/monitor_log.csv" ]; then
            echo "=== 最新5件の結果 ==="
            tail -n 5 results/monitor_log.csv
          fi
      
      - name: 結果をコミット
        if: always()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add results/
          if ! git diff --staged --quiet; then
            git commit -m "📊 監視結果更新: $(date '+%Y-%m-%d %H:%M:%S')"
            git push
          else
            echo "変更なし"
          fi
